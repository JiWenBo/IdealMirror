package com.example.dllo.idealmirror.mirrordao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.example.dllo.idealmirror.mirrordao.GoodListCache;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "GOOD_LIST_CACHE".
*/
public class GoodListCacheDao extends AbstractDao<GoodListCache, Long> {

    public static final String TABLENAME = "GOOD_LIST_CACHE";

    /**
     * Properties of entity GoodListCache.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Title = new Property(1, String.class, "title", false, "TITLE");
        public final static Property Type = new Property(2, String.class, "type", false, "TYPE");
        public final static Property Store = new Property(3, String.class, "store", false, "STORE");
        public final static Property Info_data = new Property(4, String.class, "info_data", false, "INFO_DATA");
        public final static Property TopColor = new Property(5, String.class, "topColor", false, "TOP_COLOR");
        public final static Property ButtomColor = new Property(6, String.class, "buttomColor", false, "BUTTOM_COLOR");
    };


    public GoodListCacheDao(DaoConfig config) {
        super(config);
    }
    
    public GoodListCacheDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GOOD_LIST_CACHE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"TITLE\" TEXT," + // 1: title
                "\"TYPE\" TEXT," + // 2: type
                "\"STORE\" TEXT," + // 3: store
                "\"INFO_DATA\" TEXT," + // 4: info_data
                "\"TOP_COLOR\" TEXT," + // 5: topColor
                "\"BUTTOM_COLOR\" TEXT);"); // 6: buttomColor
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GOOD_LIST_CACHE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, GoodListCache entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(2, title);
        }
 
        String type = entity.getType();
        if (type != null) {
            stmt.bindString(3, type);
        }
 
        String store = entity.getStore();
        if (store != null) {
            stmt.bindString(4, store);
        }
 
        String info_data = entity.getInfo_data();
        if (info_data != null) {
            stmt.bindString(5, info_data);
        }
 
        String topColor = entity.getTopColor();
        if (topColor != null) {
            stmt.bindString(6, topColor);
        }
 
        String buttomColor = entity.getButtomColor();
        if (buttomColor != null) {
            stmt.bindString(7, buttomColor);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public GoodListCache readEntity(Cursor cursor, int offset) {
        GoodListCache entity = new GoodListCache( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // title
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // type
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // store
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // info_data
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // topColor
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6) // buttomColor
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, GoodListCache entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTitle(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setType(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setStore(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setInfo_data(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTopColor(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setButtomColor(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(GoodListCache entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(GoodListCache entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
